import { BordesDirective } from './bordes.directive';

describe('BordesDirective', () => {
  it('should create an instance', () => {
    const directive = new BordesDirective();
    expect(directive).toBeTruthy();
  });
});
